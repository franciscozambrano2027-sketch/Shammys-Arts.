document.addEventListener('DOMContentLoaded',()=>{
 const menu=document.querySelector('.menu-toggle'), links=document.querySelector('.nav-links');
 if(menu&&links){menu.addEventListener('click',()=>{links.classList.toggle('open');menu.setAttribute('aria-expanded',links.classList.contains('open'))});links.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>links.classList.remove('open')))}
 const reveal=document.querySelectorAll('.reveal'); if('IntersectionObserver' in window){const io=new IntersectionObserver((entries,obs)=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');obs.unobserve(e.target)}}),{threshold:.08,rootMargin:'0px 0px -35px'});reveal.forEach(x=>io.observe(x))}else reveal.forEach(x=>x.classList.add('visible'));
 if(window.matchMedia('(pointer:fine)').matches){document.querySelectorAll('[data-tilt]').forEach(card=>{card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(900px) rotateX(${(-y*3).toFixed(2)}deg) rotateY(${(x*4).toFixed(2)}deg) translateY(-5px)`});card.addEventListener('pointerleave',()=>card.style.transform='')})}
 const lb=document.querySelector('.lightbox'); const lbImg=lb?.querySelector('img'); const cap=lb?.querySelector('.lightbox-caption'); let current=0, items=[];
 function openLB(index,list){if(!lb)return;items=list||[];current=index;showLB();lb.classList.add('open');document.body.style.overflow='hidden'}
 function showLB(){const el=items[current];if(!el)return;lbImg.src=el.src;lbImg.alt=el.alt||'';if(cap)cap.textContent=el.dataset.caption||el.alt||''}
 function closeLB(){lb?.classList.remove('open');document.body.style.overflow=''}
 document.querySelectorAll('[data-lightbox]').forEach(group=>{const imgs=[...group.querySelectorAll('img')];imgs.forEach((img,i)=>img.parentElement.addEventListener('click',()=>openLB(i,imgs)))})
 document.querySelectorAll('[data-single-lightbox]').forEach(img=>img.addEventListener('click',()=>openLB(0,[img])));
 lb?.querySelector('.lightbox-close')?.addEventListener('click',closeLB); lb?.querySelector('.lightbox-prev')?.addEventListener('click',()=>{current=(current-1+items.length)%items.length;showLB()});lb?.querySelector('.lightbox-next')?.addEventListener('click',()=>{current=(current+1)%items.length;showLB()});lb?.addEventListener('click',e=>{if(e.target===lb)closeLB()});document.addEventListener('keydown',e=>{if(!lb?.classList.contains('open'))return;if(e.key==='Escape')closeLB();if(e.key==='ArrowLeft'){current=(current-1+items.length)%items.length;showLB()}if(e.key==='ArrowRight'){current=(current+1)%items.length;showLB()}});
 const gallery=document.querySelector('[data-catalog-gallery]'); if(gallery){const cards=[...gallery.querySelectorAll('.gallery-card')], step=24; let shown=step; cards.forEach((c,i)=>c.hidden=i>=shown); const btn=document.querySelector('[data-load-more]'),count=document.querySelector('[data-count]'); const update=()=>{if(count)count.textContent=`Mostrando ${Math.min(shown,cards.length)} de ${cards.length} trabajos`;if(btn)btn.style.display=shown<cards.length?'inline-flex':'none'};btn?.addEventListener('click',()=>{shown=Math.min(shown+step,cards.length);cards.forEach((c,i)=>c.hidden=i>=shown);update();reveal.forEach(x=>x.classList.add('visible'))});update()}
 const filters=[...document.querySelectorAll('.filter-btn')]; if(filters.length){const cards=[...document.querySelectorAll('[data-filter-card]')];filters.forEach(btn=>btn.addEventListener('click',()=>{filters.forEach(x=>x.classList.remove('active'));btn.classList.add('active');const f=btn.dataset.filter;cards.forEach(c=>c.style.display=(f==='todos'||c.dataset.filter===f)?'':'none')}))}
 document.querySelectorAll('a[href]').forEach(a=>{const href=a.getAttribute('href');if(!href||href.startsWith('#')||href.startsWith('http')||href.startsWith('mailto:')||href.startsWith('tel:'))return;a.addEventListener('click',e=>{if(e.metaKey||e.ctrlKey||e.shiftKey||e.altKey)return;e.preventDefault();document.body.style.opacity='0';setTimeout(()=>location.href=href,130)})});
 const path=location.pathname.replace(/\\+$/,'')||'/';document.querySelectorAll('.nav-links a').forEach(a=>{const href=a.getAttribute('href')||'';if(!href.startsWith('http')&&!href.startsWith('#')){const target=href.split('#')[0].replace(/\\+$/,'');if(path.endsWith(target)||path.endsWith(target.replace('../','')))a.classList.add('active')}});
});